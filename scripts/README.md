# Scripts

These are one-time maintenance/setup scripts.

They are not part of the main application and are kept here for reference or manual execution when needed (e.g., database initialization, data migration, inventory loading, schema fixes).
